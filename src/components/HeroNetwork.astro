---
import HeroNetwork from './hero/HeroNetwork.astro';
import type { NetworkPathData } from '../types/hero';

interface Props {
  paths?: NetworkPathData[];
  showConvergence?: boolean;
  animated?: boolean;
  class?: string;
}
---

<HeroNetwork {...Astro.props} />
